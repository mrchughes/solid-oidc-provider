# Server configuration
PORT=3001
NODE_ENV=development

# OIDC Provider
ISSUER=http://localhost:3001
COOKIE_SECRET=solid_oidc_dev_cookie_secret_change_me_in_production

# JWT Keys (replace with proper keys in production)
JWT_PRIVATE_KEY_PATH=./keys/private.key
JWT_PUBLIC_KEY_PATH=./keys/public.key

# Email Service
EMAIL_SERVICE=smtp
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=user@example.com
EMAIL_PASS=password
EMAIL_FROM=no-reply@solidoidc.example.com

# Database (for future use)
# DB_URI=mongodb://localhost:27017/solid-oidc
